
# WebSoft Code Repository

Репозиторий для хранения и организации JavaScript-скриптов системы WebSoft, предназначенный для удобного использования в Obsidian и других редакторах.

## 📋 Содержание

- [Описание](#описание)
- [Структура проекта](#структура-проекта)
- [Установка и использование](#установка-и-использование)
- [Кодстайлы](#кодстайлы)
- [Архитектура](#архитектура)
- [Примеры использования](#примеры-использования)
- [Вклад в проект](#вклад-в-проект)

## 🎯 Описание

Данный репозиторий содержит JavaScript-скрипты для системы WebSoft, включая:
- **Агенты** - автоматизированные задачи и процессы
- **Серверный код** - логика страниц и API
- **Отчёты** - генерация и экспорт данных
- **Интеграции** - выгрузка данных в внешние системы

Все скрипты написаны в соответствии с внутренними кодстайлами и оптимизированы для работы с большими объёмами данных.

## 📁 Структура проекта

```
rep/
├── docs/                           # Документация
│   └── code_style.md              # Руководство по стилю кода
├── WScode/                        # Основная папка со скриптами
│   ├── agents/                    # Агенты WebSoft
│   │   ├── Агент возвращения курса.js
│   │   ├── Закрытие курсов МКР после истечения срока.js
│   │   ├── Закрытие курсов ПДД после истечения срока.js
│   │   ├── Перенос в архив групп, курсов и анкет ГГС.js
│   │   ├── Перенос в архив групп, курсов и анкет ПДД.js
│   │   ├── Перенос в архив уволенных.js
│   │   └── Агент из тестового задания, близок к кодстайлам.js
│   ├── polls/                     # Опросы и анкетирование
│   │   ├── Выгрузка опросов с разными вариантами ответов.js
│   │   └── Добавление пользователя-назначение анкеты.js
│   ├── reports/                   # Отчёты и экспорт данных
│   │   ├── Отчет для CRM(по курсу, для самообразования)/
│   │   ├── Отчет для ПП/
│   │   ├── Отчет по группе. Курс/
│   │   ├── Отчет по группе(со временем). Группа-курс/
│   │   └── Отчет по слушателю. Все курсы/
│   ├── servcode/                  # Серверный код страниц
│   │   ├── Код домашней страницы.js
│   │   ├── Код проверки и формирования сертификата.js
│   │   ├── Код страницы FAQ.js
│   │   ├── Код страницы анализа логов ошибок.js
│   │   ├── Код страницы личный кабинет.js
│   │   ├── Код страницы моё обучение.js
│   │   ├── Код страницы профилей.js
│   │   └── Код страницы самообразования.js
│   └── Выгрузка(большой проект)/  # Проект интеграции с внешней БД
│       ├── agents/                # Агенты выгрузки
│       │   ├── Завершение выгрузки и отправка сообщения.js
│       │   └── Запуск выгрузки.js
│       └── Основной код выгрузки/ # Логика экспорта данных
│           ├── Актуализация ИОГВ, завершение выгрузки, отправка сообщения.js
│           ├── Выгрузка данных в должности.js
│           └── Выгрузка сотрудников и их данных.js
└── README.md                      # Документация проекта
```

## 🚀 Установка и использование

### Предварительные требования

- Доступ к системе WebSoft
- Git для клонирования репозитория
- VSCode или другой редактор кода

### Установка

1. **Клонирование репозитория:**
   ```bash
   git clone <URL репозитория>
   cd rep
   ```

2. **Настройка рабочего окружения:**
   - Откройте папку проекта в VSCode
   - Убедитесь, что отключены валидации JS/TS и ESLint (настройки в `.vscode/settings.json`)
   - Добавьте папку `WScode` в ваш Vault Obsidian для быстрого доступа

3. **Использование скриптов:**
   - Найдите нужный скрипт в соответствующей папке
   - Скопируйте код в систему WebSoft
   - Настройте параметры согласно вашим требованиям

## 📝 Кодстайлы

Проект следует строгим внутренним кодстайлам WebSoft. Основные принципы:

### Общие правила
- **Отступы:** 2 пробела (не табы)
- **Ширина кода:** максимум 100 символов
- **Точки с запятой:** обязательны для всех выражений
- **Строки:** одинарные кавычки для строковых литералов

### Наименования
- **Функции:** `lowerCamelCase` (например: `getPersonGameRating`)
- **Переменные:** `lowerCamelCase` (например: `personID`)
- **Константы:** `CONSTANT_CASE` (например: `SECRET_NUMBER`)
- **Глобальные переменные:** запрещены (см. список в `docs/code_style.md`)

### Структура кода
- **Объект CONFIG:** все параметры выносятся в начало файла
- **Типизация:** использование JSDoc аннотаций для типов
- **Логирование:** `LogEvent` вместо `alert` в продакшене
- **Обработка ошибок:** проверка на `undefined` для функций с префиксом `Opt`

### Оптимизация
- **База данных:** минимизация обращений в циклах
- **Массивы:** использование `ArrayDirect` для больших массивов
- **XQuery:** предпочтение над SQL-запросами
- **Память:** избегание `open_doc` в циклах

## 🏗️ Архитектура

### Принципы проектирования
1. **Разделение ответственности** - каждый скрипт решает одну задачу
2. **Конфигурируемость** - параметры выносятся в объект `CONFIG`
3. **Переиспользование** - общие функции выносятся в библиотеки
4. **Логирование** - детальное отслеживание выполнения операций
5. **Обработка ошибок** - graceful degradation и информативные сообщения

### Паттерны использования
- **Агенты:** автоматизация рутинных задач
- **Серверный код:** обработка запросов и рендеринг страниц
- **Отчёты:** генерация данных в различных форматах
- **Интеграции:** обмен данными с внешними системами

## 💡 Примеры использования

### Пример агента (назначение курса)

```javascript
/**
 * Агент для назначения курса группе сотрудников
 * ---------------------------------------------------------------------------
 * Назначение: Массовое назначение курса всем сотрудникам указанной группы
 */

// Конфигурация агента
var CONFIG = {
  GROUP_ID: 7186760286810899670,           // ID группы сотрудников
  COURSE_TO_ASSIGN: 7028192885261758073    // ID курса для назначения
};

// Включаем отдельное логирование
EnableLog('course_assignment', true);

// Объявление переменных с типизацией
/** @type {XmlTopElem} Документ группы сотрудников */
var groupDoc;
/** @type {number} Счётчик успешно назначенных курсов */
var assignedCount;

// ... остальной код
```

### Пример серверного кода

```javascript
/**
 * Страница личного кабинета пользователя
 * @param {Object} Request - объект запроса
 * @param {Object} Response - объект ответа
 */

// Конфигурация страницы
var CONFIG = {
  DEFAULT_TAB: 'profile',
  ITEMS_PER_PAGE: 20
};

// Получаем параметры запроса
var currentTab = Request.QueryString('tab') || CONFIG.DEFAULT_TAB;
var pageNumber = OptInt(Request.QueryString('page')) || 1;

// ... логика страницы
```

## 🤝 Вклад в проект

### Добавление новых скриптов

1. **Создайте файл** в соответствующей папке
2. **Следуйте кодстайлам** из `docs/code_style.md`
3. **Добавьте JSDoc** документацию для всех функций
4. **Протестируйте** код перед коммитом
5. **Обновите README** при изменении структуры

### Рекомендации по разработке

- **Используйте существующие библиотеки** вместо написания с нуля
- **Добавляйте комментарии** для неочевидной логики
- **Оптимизируйте запросы** к базе данных
- **Логируйте важные события** для отладки
- **Обрабатывайте ошибки** gracefully

## 📚 Дополнительные ресурсы

- [Руководство по стилю кода](docs/code_style.md) - детальные правила кодирования
- [Документация WebSoft](https://websoft.ru/) - официальная документация системы
- [JSDoc справочник](https://jsdoc.app/) - документация по аннотациям типов

## 📄 Лицензия

Данный код предназначен для внутреннего использования в системе WebSoft. Все права защищены.

---

**Примечание:** Для запуска скриптов требуется доступ к системе WebSoft. Убедитесь, что у вас есть необходимые права доступа перед использованием.
